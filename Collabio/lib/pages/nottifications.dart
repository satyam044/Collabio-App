import 'package:flutter/material.dart';

class NottificationsPage extends StatelessWidget {
  const NottificationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Nottifications')),
    );
  }
}
